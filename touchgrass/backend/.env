# Secret key for session management (generate a secure one!)
SECRET_KEY=your-super-secret-key-here-use-a-long-random-string

# Frontend URL (comma-separated for multiple)
FRONTEND_URL=http://localhost:3001,http://localhost:5173

# Database path (optional, defaults to touchgrass.db)
DATABASE_PATH=touchgrass.db

# Flask debug mode (set to False in production!)
FLASK_DEBUG=True

# Port (optional, defaults to 5000)
PORT=5000